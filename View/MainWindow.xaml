<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation"
        xmlns:behaviors="clr-namespace:Presentation.Behaviors"
        mc:Ignorable="d"
        Title="Ballsy" Height="600" Width="900"
        ResizeMode="NoResize"
        Background="White">

    <Window.DataContext>
        <local:ViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/> <!-- Lewa kolumna: EDYTOR -->
            <ColumnDefinition Width="*"/>   <!-- Prawa kolumna: STÓŁ BILARDOWY -->
        </Grid.ColumnDefinitions>

        <!-- EDYTOR -->
        <Border Grid.Column="0" BorderBrush="Black" BorderThickness="2" Margin="10" Padding="10">
            <StackPanel VerticalAlignment="Top">
                <TextBlock Text="EDYTOR" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <TextBlock Text="Liczba kul: " VerticalAlignment="Center" FontSize="14"/>
                    <TextBox Width="60" Margin="5,0,0,0"
                             Text="{Binding Liczba, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}"/>
                </StackPanel>

                <Button Content="SYMULUJ" Command="{Binding StartCommand}" Margin="0,0,0,10"/>
                <Button Content="RESET" Command="{Binding ResetCommand}" />
            </StackPanel>
        </Border>

        <!-- STÓŁ -->
        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="2" Margin="10">
            <Canvas x:Name="BilardCanvas" Background="#142414"
                    behaviors:CanvasSizeBehavior.ObserveSize="True">
                <Ellipse Width="20" Height="20" Fill="White"/>
            </Canvas>
        </Border>
    </Grid>
</Window>
